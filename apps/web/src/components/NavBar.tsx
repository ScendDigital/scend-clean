"use client";

export default function NavBar() {
  return (
    <nav className="w-full bg-gray-100 p-4 border-b">
      <div className="max-w-6xl mx-auto flex justify-between">
        <span className="font-bold text-lg">Scend</span>
      </div>
    </nav>
  );
}

// vercel-rebuild

// vercel-rebuild
